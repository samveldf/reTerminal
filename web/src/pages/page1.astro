---
import dayjs from 'dayjs';
import BaseLayout from '../layouts/BaseLayout.astro';
import { getGoogleHeadlines } from '../services/newsService';
import { getWeather } from '../services/weatherService';

const weather = await getWeather();
const news = await getGoogleHeadlines();
const now = dayjs();
---

<BaseLayout>
  <main class="epaper-screen page1-screen">
    <header class="cyber-header">
      <div class="header-title">PAGE 1 // WEATHER + GADGET NEWS</div>
      <div class="header-time">{`TOKYO ${now.format('MM-DD HH:mm')}`}</div>
    </header>

    <div class="split-layout">
      <section class="panel weather-panel">
        <div class="section-head">TODAY + NEXT 3 DAYS</div>
        <div class="weather-grid">
          {
            weather.days.map((day) => (
              <article class="day-card">
                <div class="day-head">
                  <span>{day.label}</span>
                  <span>{day.date}</span>
                </div>
                <div class="icon-line">
                  <span class="icon-emoji">{day.icon}</span>
                  <span class="icon-symbol">{day.symbol}</span>
                </div>
                <div class="day-desc">{day.description}</div>
                <div class="day-item">{`MAX ${day.maxTemp}°C  MIN ${day.minTemp}°C`}</div>
              </article>
            ))
          }
        </div>
      </section>

      <section class="panel news-panel">
        <div class="section-head">GADGET HEADLINES</div>
        <div class="headline-list">
          {
            news.headlines.slice(0, 6).map((item, idx) => (
              <div class="headline-item">{`${idx + 1}. ${item.title}`}</div>
            ))
          }
        </div>
      </section>
    </div>

    <div class="corner-note">1 / 3</div>
  </main>
</BaseLayout>

<style>
  .page1-screen {
    gap: 6px;
    padding: 6px;
    position: relative;
  }

  .split-layout {
    flex: 1;
    min-height: 0;
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .weather-panel {
    flex: 11;
    min-height: 0;
    border-color: var(--line);
    padding: 6px;
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .news-panel {
    flex: 9;
    min-height: 0;
    border-color: var(--warn);
    padding: 6px;
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .section-head {
    height: 20px;
    color: var(--accent);
    font-size: 16px;
    font-weight: 800;
    line-height: 18px;
    letter-spacing: 1px;
  }

  .weather-grid {
    flex: 1;
    min-height: 0;
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-template-rows: 1fr 1fr;
    gap: 6px;
  }

  .day-card {
    border: 1px solid var(--warn);
    padding: 6px;
    overflow: hidden;
  }

  .day-head {
    display: flex;
    justify-content: space-between;
    color: var(--accent);
    font-size: 13px;
    font-weight: 700;
    margin-bottom: 3px;
    line-height: 14px;
  }

  .icon-line {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-bottom: 2px;
    line-height: 18px;
  }

  .icon-emoji {
    color: var(--line);
    font-size: 24px;
    line-height: 24px;
  }

  .icon-symbol {
    color: var(--line);
    font-size: 20px;
    font-weight: 800;
    line-height: 22px;
  }

  .day-desc {
    font-size: 12px;
    line-height: 14px;
    color: var(--text);
    margin-bottom: 2px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .day-item {
    font-size: 13px;
    line-height: 14px;
    color: var(--muted);
  }

  .headline-list {
    flex: 1;
    min-height: 0;
    display: grid;
    grid-template-rows: repeat(6, 1fr);
    gap: 3px;
  }

  .headline-item {
    border-left: 3px solid var(--line);
    padding-left: 7px;
    font-size: 18px;
    line-height: 25px;
    color: var(--text);
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
  }

  .corner-note {
    position: absolute;
    right: 8px;
    bottom: 6px;
    color: var(--muted);
    font-size: 12px;
    font-weight: 700;
    background: rgba(5, 7, 10, 0.6);
    border: 1px solid var(--line);
    padding: 1px 6px;
    line-height: 16px;
  }
</style>
