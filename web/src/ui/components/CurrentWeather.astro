---
import WeatherIcon from '../parts/WeatherIcon.astro';

interface Props {
  icon: string;
  temperature: string;
  description: string;
}

const { icon, temperature, description } = Astro.props;
---

<div class="flex gap-2">
  <WeatherIcon size="large" icon={icon} />

  <div class="flex flex-col justify-center gap-4">
    <div class="text-6xl">{`${temperature}°C`}</div>
    <div class="flex justify-center text-xl">{description}</div>
  </div>
</div>
